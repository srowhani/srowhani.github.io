
import { atob } from 'portfolio/helpers/atob';
import { module, test } from 'qunit';

module('Unit | Helper | atob');

// Replace this with your real tests.
test('it works', function(assert) {
  let result = atob([42]);
  assert.ok(result);
});

